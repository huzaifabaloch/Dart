// Every dart program starts with the main function.
main() {
  // here we use type inferenece.
  // dart compiler automatically identify the type.
  // dynamic type.
  var firstName = 'Huzaifa';

  // static type.
  String lastName = 'Baloch';

  print(firstName + ' ' + lastName);
}
